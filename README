INSTRUCTIONS:

In order to run the code, we advise you to change the directory in the code where you store your CSV file.

To compile the code, user can type "python project.py" or "python3 project.py" in the terminal.
